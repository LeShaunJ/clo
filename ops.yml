dependencies:
  custom:
    - pip3 install -e .
actions:
  api: clo
  readme:
    command: |-
      unset OD_INST OD_DATA OD_USER OD_PASS
      clo --readme --out README.md
options:
  exec:
    load_secrets: true
